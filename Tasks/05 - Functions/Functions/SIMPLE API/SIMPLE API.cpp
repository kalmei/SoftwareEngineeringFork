#include "API.h"


int main() {
	int x = 30;
	int y = 40;


	displayVal(x, y);
	addVal(x, y);
	subVal(x, y);
	mulVal(x, y);
	divVal(x, y);

	flipVal(x, y);
	displayVal(x, y);
	addVal(x, y);
	subVal(x, y);
	mulVal(x, y);
	divVal(x, y);

	return 0;

}